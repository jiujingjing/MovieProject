<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.u2androidproject">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />

    <application
        android:name=".app.MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TotalStarActivity" />
        <activity android:name=".StarInfoActivity" />
        <activity android:name=".StarStatePageActivity" />
        <activity android:name=".ItemMovieActivity" />
        <activity android:name=".VideoActivity" />
        <activity android:name=".SearchMovieActivity" />
        <activity android:name=".Main2Activity" />
        <activity android:name=".ItemMyTalkActivity" />
        <activity android:name=".DragImgActivity" />
        <activity android:name=".LoginActivity"></activity>
    </application>

</manifest>